<Window x:Class="Vanta.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Vanta" Width="1000" Height="650" MinWidth="900" MinHeight="600"
        WindowStartupLocation="CenterScreen" WindowStyle="None"
        Background="{StaticResource BackgroundBrush}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="40" ResizeBorderThickness="6" CornerRadius="12" GlassFrameThickness="0" />
    </shell:WindowChrome.WindowChrome>

    <Border BorderBrush="#3A3A3D" BorderThickness="1" CornerRadius="12" Background="{StaticResource BackgroundBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="48" />
                <RowDefinition Height="*" />
                <RowDefinition Height="32" />
            </Grid.RowDefinitions>

            <!-- title bar -->
            <Border Grid.Row="0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Background="#101011" CornerRadius="12,12,0,0">
                <Grid Margin="11,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="220" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="220" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <Grid Width="27" Height="27" Margin="2,0,12,0">
                            <Path Fill="#FFFFFF" Data="M 2,3 L 10,3 L 16,20 L 21,3 L 29,3 L 21,25 L 16,15 L 8,25 Z" Stretch="Uniform" />
                        </Grid>
                        <RadioButton x:Name="DashboardNav" GroupName="Navigation" Style="{StaticResource NavButton}" Content="&#xE80F;" IsChecked="True" ToolTip="Clicker" Click="DashboardNav_Click" />
                        <RadioButton x:Name="SequenceNav" GroupName="Navigation" Style="{StaticResource NavButton}" Content="&#xE81E;" ToolTip="Sequence" Click="SequenceNav_Click" />
                        <RadioButton x:Name="SettingsNav" GroupName="Navigation" Style="{StaticResource NavButton}" Content="&#xE713;" ToolTip="Settings" Click="SettingsNav_Click" />
                    </StackPanel>

                    <TextBlock Grid.Column="1" Text="V A N T A" FontSize="13" FontWeight="SemiBold" Foreground="#CFCFD2" HorizontalAlignment="Center" VerticalAlignment="Center" />

                    <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button x:Name="PinButton" Style="{StaticResource IconButton}" Width="38" Height="34" ToolTip="Always on top" Click="PinButton_Click">
                            <TextBlock Text="&#xE718;" FontFamily="Segoe Fluent Icons" FontSize="13" Foreground="{StaticResource MutedBrush}" />
                        </Button>
                        <Button Style="{StaticResource IconButton}" Width="38" Height="34" ToolTip="Minimize" Click="MinimizeButton_Click">
                            <TextBlock Text="&#xE921;" FontFamily="Segoe Fluent Icons" FontSize="13" />
                        </Button>
                        <Button Style="{StaticResource IconButton}" Width="38" Height="34" ToolTip="Close" Click="CloseButton_Click">
                            <TextBlock Text="&#xE8BB;" FontFamily="Segoe Fluent Icons" FontSize="13" />
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- dashboard -->
            <Grid x:Name="DashboardView" Grid.Row="1" Margin="20,18,20,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.08*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="0.92*" />
                </Grid.ColumnDefinitions>

                <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel>
                        <Border Style="{StaticResource SectionCard}">
                            <StackPanel>
                                <Grid Margin="0,0,0,12">
                                    <TextBlock Text="Cadence" FontSize="14" FontWeight="SemiBold" />
                                    <TextBlock Text="ⓘ" Foreground="{StaticResource MutedBrush}" HorizontalAlignment="Right" ToolTip="Choose a rate or exact delay." />
                                </Grid>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="90" />
                                        <ColumnDefinition Width="12" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="12" />
                                        <ColumnDefinition Width="150" />
                                    </Grid.ColumnDefinitions>
                                    <TextBox x:Name="CadenceValueBox" Grid.Column="0" Text="10" PreviewTextInput="NumberOnly_PreviewTextInput" TextChanged="Setting_Changed" />
                                    <ComboBox x:Name="CadenceUnitCombo" Grid.Column="2" SelectionChanged="Setting_Changed">
                                        <ComboBoxItem Content="Clicks / second" IsSelected="True" />
                                        <ComboBoxItem Content="Milliseconds" />
                                    </ComboBox>
                                    <Border Grid.Column="4" Background="#202022" CornerRadius="7" Padding="3">
                                        <UniformGrid Columns="2">
                                            <RadioButton x:Name="RateModeRadio" GroupName="CadenceMode" Style="{StaticResource PillRadio}" Content="Rate" IsChecked="True" Checked="Setting_Changed" />
                                            <RadioButton x:Name="DelayModeRadio" GroupName="CadenceMode" Style="{StaticResource PillRadio}" Content="Delay" Checked="Setting_Changed" />
                                        </UniformGrid>
                                    </Border>
                                </Grid>
                                <Grid Margin="0,10,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="90" />
                                        <ColumnDefinition Width="12" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="12" />
                                        <ColumnDefinition Width="150" />
                                    </Grid.ColumnDefinitions>
                                    <ComboBox x:Name="ModifierCombo" Grid.Column="0" SelectionChanged="Hotkey_Changed">
                                        <ComboBoxItem Content="Alt" IsSelected="True" />
                                        <ComboBoxItem Content="Ctrl" />
                                        <ComboBoxItem Content="Shift" />
                                        <ComboBoxItem Content="Win" />
                                    </ComboBox>
                                    <ComboBox x:Name="HotKeyCombo" Grid.Column="2" SelectionChanged="Hotkey_Changed" />
                                    <Border Grid.Column="4" Background="#202022" CornerRadius="7" Padding="3">
                                        <UniformGrid Columns="2">
                                            <RadioButton x:Name="ToggleModeRadio" GroupName="ActivationMode" Style="{StaticResource PillRadio}" Content="Toggle" IsChecked="True" Checked="Setting_Changed" />
                                            <RadioButton x:Name="HoldModeRadio" GroupName="ActivationMode" Style="{StaticResource PillRadio}" Content="Hold" Checked="Setting_Changed" />
                                        </UniformGrid>
                                    </Border>
                                </Grid>
                                <Grid Margin="0,10,0,0">
                                    <TextBlock Text="Mouse button" Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center" />
                                    <Border Background="#202022" CornerRadius="7" Padding="3" HorizontalAlignment="Right" Width="250">
                                        <UniformGrid Columns="3">
                                            <RadioButton x:Name="LeftButtonRadio" GroupName="MouseButton" Style="{StaticResource PillRadio}" Content="Left" IsChecked="True" Checked="Setting_Changed" />
                                            <RadioButton x:Name="MiddleButtonRadio" GroupName="MouseButton" Style="{StaticResource PillRadio}" Content="Middle" Checked="Setting_Changed" />
                                            <RadioButton x:Name="RightButtonRadio" GroupName="MouseButton" Style="{StaticResource PillRadio}" Content="Right" Checked="Setting_Changed" />
                                        </UniformGrid>
                                    </Border>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource SectionCard}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="145" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="Click duration" FontWeight="SemiBold" />
                                    <TextBlock Text="Portion of each interval the button stays down" FontSize="11" Foreground="{StaticResource MutedBrush}" Margin="0,4,0,0" />
                                </StackPanel>
                                <Grid Grid.Column="1">
                                    <Slider x:Name="DurationSlider" Minimum="1" Maximum="100" Value="15" TickFrequency="1" ValueChanged="DurationSlider_ValueChanged" />
                                    <TextBlock x:Name="DurationText" Text="15%" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-18,0,0" Foreground="{StaticResource MutedBrush}" />
                                </Grid>
                            </Grid>
                        </Border>

                        <Border Style="{StaticResource SectionCard}">
                            <StackPanel>
                                <Grid>
                                    <StackPanel>
                                        <TextBlock Text="Limits" FontWeight="SemiBold" />
                                        <TextBlock Text="Stop automatically after a target" FontSize="11" Foreground="{StaticResource MutedBrush}" Margin="0,4,0,0" />
                                    </StackPanel>
                                    <CheckBox x:Name="LimitEnabledCheck" HorizontalAlignment="Right" Content="Enabled" Checked="Setting_Changed" Unchecked="Setting_Changed" />
                                </Grid>
                                <Grid Margin="0,12,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="12" />
                                        <ColumnDefinition Width="150" />
                                    </Grid.ColumnDefinitions>
                                    <TextBox x:Name="LimitValueBox" Text="1000" PreviewTextInput="NumberOnly_PreviewTextInput" TextChanged="Setting_Changed" />
                                    <ComboBox x:Name="LimitTypeCombo" Grid.Column="2" SelectionChanged="Setting_Changed">
                                        <ComboBoxItem Content="Clicks" IsSelected="True" />
                                        <ComboBoxItem Content="Seconds" />
                                    </ComboBox>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource SectionCard}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="135" />
                                    <ColumnDefinition Width="110" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="Speed variation" FontWeight="SemiBold" />
                                    <TextBlock Text="Randomizes timing naturally" FontSize="11" Foreground="{StaticResource MutedBrush}" Margin="0,4,0,0" />
                                </StackPanel>
                                <Grid Grid.Column="1" Margin="0,0,15,0">
                                    <Slider x:Name="VariationSlider" Minimum="0" Maximum="50" Value="10" ValueChanged="VariationSlider_ValueChanged" />
                                    <TextBlock x:Name="VariationText" Text="10%" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-18,0,0" Foreground="{StaticResource MutedBrush}" />
                                </Grid>
                                <CheckBox x:Name="VariationEnabledCheck" Grid.Column="2" HorizontalAlignment="Right" Content="Enabled" Checked="Setting_Changed" Unchecked="Setting_Changed" />
                            </Grid>
                        </Border>

                        <Border Style="{StaticResource SectionCard}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="90" />
                                    <ColumnDefinition Width="110" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="Double click" FontWeight="SemiBold" />
                                    <TextBlock Text="Sends two clicks per interval" FontSize="11" Foreground="{StaticResource MutedBrush}" Margin="0,4,0,0" />
                                </StackPanel>
                                <TextBox x:Name="DoubleClickGapBox" Grid.Column="1" Text="50" ToolTip="Gap in milliseconds" PreviewTextInput="NumberOnly_PreviewTextInput" TextChanged="Setting_Changed" />
                                <CheckBox x:Name="DoubleClickEnabledCheck" Grid.Column="2" HorizontalAlignment="Right" Content="Enabled" Checked="Setting_Changed" Unchecked="Setting_Changed" />
                            </Grid>
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                <!-- sequence panel -->
                <Border Grid.Column="2" Style="{StaticResource SectionCard}" Margin="0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid>
                            <StackPanel>
                                <TextBlock Text="Sequence clicking" FontSize="14" FontWeight="SemiBold" />
                                <TextBlock Text="Cycle through saved screen positions" FontSize="11" Foreground="{StaticResource MutedBrush}" Margin="0,4,0,0" />
                            </StackPanel>
                            <CheckBox x:Name="SequenceEnabledCheck" HorizontalAlignment="Right" Content="Enabled" Checked="Setting_Changed" Unchecked="Setting_Changed" />
                        </Grid>
                        <Button Grid.Row="1" Style="{StaticResource SecondaryButton}" Content="＋  Add current cursor" Margin="0,16,0,12" Click="AddCursorButton_Click" />
                        <ListBox x:Name="SequenceList" Grid.Row="2" Background="#101011" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Foreground="{StaticResource TextBrush}" Padding="8" />
                        <StackPanel Grid.Row="3" Margin="0,12,0,0">
                            <Button Style="{StaticResource SecondaryButton}" Content="Remove selected" Click="RemoveCursorButton_Click" />
                            <TextBlock Text="Positions are visited in order and repeat." FontSize="11" Foreground="{StaticResource MutedBrush}" TextAlignment="Center" Margin="0,10,0,0" />
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>

            <!-- settings -->
            <ScrollViewer x:Name="SettingsView" Grid.Row="1" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                <Grid Margin="28,24,28,18" MaxWidth="850">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="16" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Settings" FontSize="26" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <TextBlock Text="Keep Vanta working the way you like." Foreground="{StaticResource MutedBrush}" Margin="0,0,0,20" />

                        <Border Style="{StaticResource SectionCard}">
                            <StackPanel>
                                <TextBlock Text="General" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,14" />
                                <CheckBox x:Name="StartWithWindowsCheck" Content="Start Vanta when I sign in" Margin="0,0,0,12" Checked="StartWithWindows_Changed" Unchecked="StartWithWindows_Changed" />
                                <CheckBox x:Name="RememberWindowCheck" Content="Remember window position" Margin="0,0,0,12" Checked="Setting_Changed" Unchecked="Setting_Changed" />
                                <CheckBox x:Name="MinimizeOnStartCheck" Content="Start minimized" Checked="Setting_Changed" Unchecked="Setting_Changed" />
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource SectionCard}">
                            <StackPanel>
                                <TextBlock Text="Updates" FontSize="14" FontWeight="SemiBold" />
                                <TextBlock x:Name="UpdateStatusText" Text="Check GitHub Releases for a newer version." Foreground="{StaticResource MutedBrush}" FontSize="11" TextWrapping="Wrap" Margin="0,5,0,14" />
                                <Button x:Name="CheckUpdateButton" Style="{StaticResource PrimaryButton}" Content="Check for updates" Click="CheckUpdateButton_Click" />
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource SectionCard}">
                            <StackPanel>
                                <TextBlock Text="Maintenance" FontSize="14" FontWeight="SemiBold" />
                                <TextBlock Text="Reset only removes your saved Vanta preferences." Foreground="{StaticResource MutedBrush}" FontSize="11" TextWrapping="Wrap" Margin="0,5,0,14" />
                                <Button Style="{StaticResource SecondaryButton}" Content="Reset all settings" Margin="0,0,0,8" Click="ResetSettingsButton_Click" />
                                <Button Style="{StaticResource SecondaryButton}" Content="Open Windows uninstall" Click="UninstallButton_Click" />
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Margin="0,55,0,0">
                        <Border Style="{StaticResource SectionCard}">
                            <StackPanel>
                                <TextBlock Text="Click test pad" FontSize="14" FontWeight="SemiBold" />
                                <TextBlock Text="Point your cursor here, then use the hotkey." Foreground="{StaticResource MutedBrush}" FontSize="11" Margin="0,5,0,14" />
                                <Border x:Name="TestPad" Height="210" Background="#101B13" BorderBrush="#326A3E" BorderThickness="1" CornerRadius="10" PreviewMouseDown="TestPad_PreviewMouseDown">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock Text="&#xE7C9;" FontFamily="Segoe Fluent Icons" Foreground="{StaticResource AccentBrush}" FontSize="30" HorizontalAlignment="Center" />
                                        <TextBlock x:Name="TestCountText" Text="0 clicks" FontSize="22" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,12,0,0" />
                                        <TextBlock x:Name="TestLastText" Text="Waiting for input" Foreground="{StaticResource MutedBrush}" HorizontalAlignment="Center" Margin="0,5,0,0" />
                                    </StackPanel>
                                </Border>
                                <Button Style="{StaticResource SecondaryButton}" Content="Clear test" Margin="0,12,0,0" Click="ClearTestButton_Click" />
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource SectionCard}">
                            <StackPanel>
                                <TextBlock Text="About Vanta" FontSize="14" FontWeight="SemiBold" />
                                <TextBlock x:Name="VersionText" Text="Version 0.1.0" Foreground="{StaticResource MutedBrush}" Margin="0,6,0,0" />
                                <TextBlock Text="Native Windows auto clicker. No telemetry, ads, or background service." Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" FontSize="11" Margin="0,7,0,0" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </ScrollViewer>

            <!-- status bar -->
            <Border Grid.Row="2" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Background="#101011" CornerRadius="0,0,12,12">
                <Grid Margin="13,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Ellipse x:Name="StatusDot" Width="7" Height="7" Fill="{StaticResource MutedBrush}" Margin="0,0,7,0" />
                        <TextBlock x:Name="StatusText" Text="Ready · Alt + Q" Foreground="{StaticResource MutedBrush}" FontSize="11" />
                    </StackPanel>
                    <TextBlock x:Name="ClickCountText" Text="0 clicks" Foreground="{StaticResource MutedBrush}" FontSize="11" HorizontalAlignment="Right" VerticalAlignment="Center" />
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
